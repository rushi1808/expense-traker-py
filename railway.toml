[build]
builder = "nixpacks"
buildCommand = "cd python_version && pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"

[deploy]
startCommand = "cd python_version && gunicorn expense_tracker.wsgi --bind 0.0.0.0:$PORT"
